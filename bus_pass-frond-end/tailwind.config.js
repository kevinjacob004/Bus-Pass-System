export const content = [
  './src/**/*.{js,jsx,ts,tsx}', // All React components
  './public/index.html', // Include index.html if needed
];
export const theme = {
  extend: {
    screens: {
      'xs': '480px',
       // Extra small devices
    },
  }, // Extend the default theme here if necessary
};
export const plugins = [];
